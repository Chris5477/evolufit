import { BrowserRouter, Routes, Route } from "react-router-dom";

const App = () => {
  return (
    <div className='App container'>
      <BrowserRouter>
        <Routes></Routes>
      </BrowserRouter>
    </div>
  );
};

export default App;
